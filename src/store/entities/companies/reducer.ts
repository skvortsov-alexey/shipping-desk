import { CompaniesState } from './types'

const companiesReducer = (state: CompaniesState = {}, action: any): CompaniesState => {
	switch (action.type) {
		default:
			return state
	}
}

export default companiesReducer